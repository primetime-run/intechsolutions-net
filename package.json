{
  "name": "intechsolutions-net",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-sw.mjs",
    "preview": "astro preview",
    "verify-urls": "node scripts/verify-urls.mjs",
    "icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.13.0"
  }
}
